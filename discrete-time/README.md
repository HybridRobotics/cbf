#### Environments
* Create your environment via `conda env create -f environment.yml`. The default conda environment name is `duality-cbf`, and you could also choose that name with your own preferences by editing the .yml file.

#### Testing
* Run `python test.py`